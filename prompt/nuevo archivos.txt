Quiero reorganizar completamente los tipos de recursos. 

NUEVOS TIPOS (reemplazar los anteriores):
- video → igual que antes
- presentation → igual que antes  
- document → nuevo (reemplaza 'text') — editor de texto enriquecido O subida de archivo DOC/DOCX/TXT desde computadora O link de Google Drive
- pdf → nuevo (separado de presentation) — subida de PDF desde computadora O link de Google Drive, se muestra con navegación de páginas
- quiz → igual que antes

ELIMINAR:
- file (archivo descargable) — eliminarlo completamente de tipos, formularios y viewers

CAMBIOS EN types/index.ts:
- Renombrar 'text' a 'document' 
- Agregar tipo 'pdf' separado de 'presentation'
- Eliminar 'file'

CAMBIOS EN ResourceForm.tsx para CADA tipo:
Todos los tipos excepto quiz deben tener DOS opciones de carga:
1. Subir desde computadora (input file con el accept correcto)
2. Pegar link de Google Drive manualmente
Con un toggle o tabs para elegir entre las dos opciones.

Accepts por tipo:
- video: accept="video/*"
- presentation: accept=".ppt,.pptx" (PPT/PPTX solamente, PDF tiene su propio tipo)
- document: accept=".doc,.docx,.txt"
- pdf: accept=".pdf"

CAMBIOS EN ResourceViewer.tsx:
- 'document': mostrar contenido de texto si es texto, o iframe de Drive si tiene driveUrl
- 'pdf': usar el componente de navegación por páginas que ya creamos
- Eliminar el viewer de 'file'

CAMBIOS EN componentes del sidebar (ResourceStatus, íconos):
- Actualizar íconos para los nuevos tipos:
  - video: 🎬
  - presentation: 📊
  - document: 📄
  - pdf: 📋
  - quiz: ❓

Mantené compatibilidad con recursos existentes en Firestore que tengan 
el tipo 'text' o 'file' — mostrarlos sin error aunque ya no se puedan crear nuevos.