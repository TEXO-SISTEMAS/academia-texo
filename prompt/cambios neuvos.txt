Tengo tres problemas urgentes a resolver:

1. LOGO NO CARGA EN LOGIN ni en BIENVENIDA:
El archivo es logo.png pero el código busca logo.svg en algunos lugares.
Por favor:
- Buscar en TODOS los archivos cualquier referencia a '/logo.svg' 
  y cambiarla a '/logo.png'
- En app/login/page.tsx el logo debe usar:
  <img src="/logo.png" alt="Academia TEXO" style={{ height: '64px', width: 'auto' }} />
- En app/bienvenida/page.tsx igual pero height: '80px'

2. LOGIN REDIRIGE A SÍ MISMO:
Al hacer clic en "Ingresar" vuelve a la página de login en lugar 
de ir a bienvenida.
En lib/auth-context.tsx, la función login() después de 
signInWithCustomToken debe hacer:
  if (role === 'participante') router.push('/bienvenida')
  if (role === 'artesano') router.push('/artesano/dashboard')
Verificá que este código exista y no haya ningún redirect 
del middleware que esté anulándolo.

3. BIENVENIDA TARDA MUCHO:
En app/login/page.tsx agregar al montar el componente:
  import { useRouter } from 'next/navigation'
  const router = useRouter()
  useEffect(() => { router.prefetch('/bienvenida') }, [])
Esto pre-carga la página de bienvenida mientras el usuario 
escribe su email, así cuando llega ya está lista.