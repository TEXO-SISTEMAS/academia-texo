En ResourceViewer.tsx, el visor de PDF/documento/presentación 
no muestra la página completa — está cortado y no hay scroll.

El problema: el contenedor tiene overflow: hidden y height fija
que corta el contenido.

Por favor:

1. En TrackedPageViewer, el contenedor de la imagen debe:
   - Eliminar overflow: hidden del contenedor de la imagen
   - Cambiar height fija a height: auto
   - La imagen debe mostrarse completa sin recorte:
     <img 
       src={pages[currentPage - 1]} 
       style={{ 
         width: '100%', 
         height: 'auto',
         display: 'block',
         borderRadius: '8px'
       }} 
     />

2. El contenedor externo del TrackedPageViewer:
   - Eliminar cualquier maxHeight o height fija
   - Usar height: auto para que se adapte al contenido
   - overflow: visible (no hidden)

3. En la página del curso /participante/cursos/[courseId]/page.tsx:
   - El área de contenido principal (donde se muestra el recurso)
   - Cambiar overflow: hidden a overflow-y: auto cuando el tipo 
     de recurso es pdf, document o presentation
   - Así el usuario puede hacer scroll para ver toda la página

4. En móvil: la imagen debe ocupar el 100% del ancho disponible
   con height: auto para mantener proporciones

git add .
git commit -m "fix: show full PDF pages without clipping"
git push origin main