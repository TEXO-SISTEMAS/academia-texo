Todavía hay una escritura a Firestore cuando el usuario completa un recurso.

DESHABILITAR COMPLETAMENTE TODO GUARDADO:

1. En lib/firestore.ts, buscar la función markResourceCompleted
   y COMENTAR todo el contenido, reemplazar por:
   
   export async function markResourceCompleted(...) {
     // TEMPORALMENTE DESHABILITADO - quota exceeded
     console.log('[Progress] Guardado deshabilitado temporalmente')
     return
   }

2. Verificar que NO haya otras funciones que escriban a Firestore:
   - updateResourceProgress
   - updateResourceEngagement
   - recordLoginBackground
   
   COMENTAR el cuerpo de todas ellas también.

3. El usuario podrá navegar normalmente entre recursos,
   pero el progreso NO se guardará en Firestore (solo en memoria local).

git add .
git commit -m "emergency: disable all Firestore writes"
git push origin main