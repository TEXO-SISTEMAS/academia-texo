Tengo dos problemas en ResourceViewer.tsx:

1. BOTONES NO APARECEN sin ingresar cantidad de páginas manualmente:
   Para PDFs: el sistema ya convierte el PDF a imágenes via /api/drive/slides
   y retorna el array. Usar slides.length como totalPages automáticamente,
   sin necesitar que el Artesano ingrese el número manualmente.
   
   En PdfResource:
   - Cargar las slides via /api/drive/slides?fileId=XXX
   - Usar slides.length como totalPages
   - Mostrar navegación automáticamente con ese número

2. SCROLL SIGUE FUNCIONANDO — hay que deshabilitarlo completamente:
   
   Para PDFs:
   - El contenedor del iframe o imagen debe tener:
     overflow: 'hidden'
     height: '600px' (fijo, no auto)
   - Si usa iframe: agregar scrolling="no" al iframe
   - Si muestra imágenes: cada imagen ocupa exactamente el contenedor,
     sin overflow

   Para Documentos:
   - El contenedor del texto debe tener:
     overflow: 'hidden'
     maxHeight: '500px'
   - El texto NO debe ser scrolleable
   - Solo se puede navegar con los botones ← / →

3. Para AMBOS tipos — el contenedor principal debe tener:
   style={{ overflow: 'hidden', userSelect: 'none' }}
   Así el participante no puede hacer scroll ni seleccionar texto para saltear.