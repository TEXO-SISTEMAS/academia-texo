El upload de archivos pesados a Drive funcionaba antes pero ahora da error 413.

DIAGNÓSTICO:

1. Verificar que el sistema de upload resumable (chunked upload) esté activo:
   - /api/drive/resumable-init/route.ts debe inicializar la sesión
   - /api/drive/upload-chunk/route.ts debe procesar chunks de 256KB
   - El archivo grande se sube en pedazos pequeños, no todo de una vez

2. Mostrar el código del componente que hace el upload de archivos:
   - ¿Está usando resumable upload o upload simple?
   - ¿Cuándo usa uno vs otro?

3. Verificar en /app/artesano/cursos/[courseId]/page.tsx 
   (o donde esté el formulario de upload):
   - La función handleFileUpload
   - Si detecta archivos > 5MB para usar resumable
   - Si los chunks se están enviando correctamente

4. Revisar el commit donde se rompió - buscar cambios en:
   - Las rutas /api/drive/*
   - El componente de upload de archivos

Mostrame:
- El código completo de la función que sube archivos
- Los logs de la consola cuando intentás subir un archivo pesado