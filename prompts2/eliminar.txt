En lib/firestore.ts, las funciones deleteCourse, deleteChapter 
y deleteResource no están registrando en el auditLog.

Por favor mostrá el código actual de estas 3 funciones 
y verificá que cada una llame a silentAudit ANTES del updateDoc.

Si no tienen silentAudit, agregarlo:

deleteCourse:
await silentAudit(userId, userEmail, 'content', 
  `Archivó propedéutico: ${courseTitle}`, 'course', courseTitle)

deleteChapter:
await silentAudit(userId, userEmail, 'content',
  `Archivó capítulo: ${chapterTitle}`, 'chapter', chapterTitle)

deleteResource:
await silentAudit(userId, userEmail, 'content',
  `Archivó recurso: ${resourceTitle}`, 'resource', resourceTitle)

IMPORTANTE: las funciones delete deben recibir los parámetros 
userId y userEmail además de los IDs actuales.
Si no los reciben, obtenerlos de getAuth().currentUser dentro de la función.

También corregir el userEmail vacío:
const auth = getAuth()
const userEmail = auth.currentUser?.email ?? ''
const userId = auth.currentUser?.uid ?? ''

git add .
git commit -m "fix: add audit log to delete functions"
git push origin main