Crear la estructura base del dashboard de artesano con 2 pestañas.

PASO 1 - Instalar recharts:
Agregar al package.json:
"recharts": "^2.10.3"

Ejecutar: npm install

PASO 2 - Crear /app/artesano/dashboard/page.tsx:

'use client'

import { useState } from 'react'

export default function ArtesanoDashboard() {
  const [activeTab, setActiveTab] = useState<'propedeuticos' | 'participantes'>('propedeuticos')
  const [searchQuery, setSearchQuery] = useState('')

  return (
    <div className="min-h-screen bg-gray-50 p-8">
      <h1 className="text-3xl font-bold text-[#31484E] mb-6">
        Dashboard de Estadísticas
      </h1>

      {/* Tabs */}
      <div className="flex gap-4 mb-6 border-b border-gray-300">
        <button
          onClick={() => setActiveTab('propedeuticos')}
          className={`px-6 py-3 font-semibold transition-colors ${
            activeTab === 'propedeuticos'
              ? 'border-b-4 border-[#E8B84B] text-[#E8B84B]'
              : 'text-gray-600 hover:text-gray-900'
          }`}
        >
          Por Propedéutico
        </button>
        <button
          onClick={() => setActiveTab('participantes')}
          className={`px-6 py-3 font-semibold transition-colors ${
            activeTab === 'participantes'
              ? 'border-b-4 border-[#E8B84B] text-[#E8B84B]'
              : 'text-gray-600 hover:text-gray-900'
          }`}
        >
          Por Participante
        </button>
      </div>

      {/* Buscador */}
      <div className="mb-6">
        <input
          type="text"
          placeholder={
            activeTab === 'propedeuticos'
              ? 'Buscar propedéutico...'
              : 'Buscar participante por nombre o email...'
          }
          value={searchQuery}
          onChange={(e) => setSearchQuery(e.target.value)}
          className="w-full max-w-md px-4 py-2 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-[#3A9688]"
        />
      </div>

      {/* Contenido de las tabs */}
      {activeTab === 'propedeuticos' ? (
        <PropedeuticosView searchQuery={searchQuery} />
      ) : (
        <ParticipantesView searchQuery={searchQuery} />
      )}
    </div>
  )
}

function PropedeuticosView({ searchQuery }: { searchQuery: string }) {
  return (
    <div>
      <h2 className="text-xl font-bold mb-4">Vista por Propedéutico</h2>
      <p className="text-gray-600">Gráficos en construcción...</p>
    </div>
  )
}

function ParticipantesView({ searchQuery }: { searchQuery: string }) {
  return (
    <div>
      <h2 className="text-xl font-bold mb-4">Vista por Participante</h2>
      <p className="text-gray-600">Gráficos en construcción...</p>
    </div>
  )
}

PASO 3 - Commit y push:
git add .
git commit -m "feat: dashboard base structure with tabs and search"
git push origin main