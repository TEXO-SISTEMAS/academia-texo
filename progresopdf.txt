Necesito implementar progreso parcial en recursos tipo PDF.

Contexto actual:
- Los PDFs se muestran página por página con un timer de 60 segundos por página
- El recurso se marca completo solo cuando se ven TODAS las páginas
- La barra de progreso superior solo se actualiza cuando se completa el recurso entero

Cambios necesarios:

1. GUARDAR PROGRESO PARCIAL POR PÁGINA:
   En TrackedPageViewer (components/TrackedPageViewer.tsx):
   - Cuando el timer de 60 segundos se completa en una página,
     guardar en Firestore: progress/{userId}/courses/{courseId}/resources/{resourceId}
     con el campo: lastCompletedPage: número de página actual
   - Esto debe ocurrir en segundo plano, sin bloquear la navegación

2. ACTUALIZAR BARRA DE PROGRESO:
   En /app/participante/cursos/[courseId]/page.tsx:
   - Modificar el cálculo de coursePct para considerar páginas parciales:
     * Si un PDF tiene 5 páginas y el usuario vio 3 completas (con timer),
       contar como 3/5 = 0.6 recursos en lugar de 0
     * Fórmula: completedResources = suma de (lastCompletedPage / totalPages) 
       para PDFs + recursos completos normales
   - La barra debe actualizarse en tiempo real cuando se completa cada página

3. CARGAR PROGRESO AL ENTRAR:
   - Al abrir un PDF, si existe lastCompletedPage en Firestore,
     mostrar un mensaje: "Continuás desde la página X"
   - Pero el usuario puede navegar libremente a cualquier página

4. SIN CAMBIOS EN LA LÓGICA DE "COMPLETADO":
   - El recurso sigue marcándose como completed: true solo cuando
     se ven TODAS las páginas
   - El progreso parcial es solo para la barra superior

git add .
git commit -m "feat: partial progress tracking for PDF pages"
git push origin main