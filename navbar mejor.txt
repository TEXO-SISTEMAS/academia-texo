Mejorar el dashboard del Participante /participante/dashboard/page.tsx:

1. BÚSQUEDA Y ORDENAMIENTO:
   - Agregar barra de búsqueda arriba de las cards: "Buscar propedéutico..."
   - Filtrar cards en tiempo real por título mientras escribe
   - Selector de ordenamiento con opciones:
     * "Todos" (por defecto)
     * "En progreso" (progreso > 0 y < 100%)
     * "Sin iniciar" (progreso === 0)
     * "Completados" (progreso === 100%)

2. BOTÓN MÁS VISIBLE EN LAS CARDS:
   - En CourseCard.tsx agregar un botón en la parte inferior de la card:
     * Si progreso === 0: botón "Empezar →" fondo texo-amarillo
     * Si progreso > 0 y < 100%: botón "Continuar →" fondo texo-verde
     * Si progreso === 100%: botón "Repasar →" fondo gris
   - El botón ocupa todo el ancho de la card

3. BADGE COMPLETADO:
   - Si progreso === 100%: mostrar badge "✓ Completado" 
     en la esquina superior derecha de la imagen
     fondo texo-verde, texto blanco, border-radius pill

4. NOMBRE EN NAVBAR:
   - En components/shared/Navbar.tsx mostrar el nombre del usuario
     (campo name de allowedUsers, o la parte antes del @ del email)
     junto al toggle de dark/light mode
   - Formato: "Hola, Danilo" en texto pequeño color blanco/muted

git add .
git commit -m "feat: dashboard search, sort, better cards, name in navbar"
git push origin main